<% if @user.id == current_user.id %>
<h1>Edit User</h1>
<%= form_for @user do |f| %>
<%= render :partial =>'common/form', :locals=> {:f=>f} %>
 <% end %>
<%= link_to 'Back', user_path(@user) %>
 <% end %>