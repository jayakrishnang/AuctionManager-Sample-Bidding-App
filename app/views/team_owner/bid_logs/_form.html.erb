<%= form_for(@bid_log) do |f| %>
  <% if @bid_log.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bid_log.errors.count, "error") %> prohibited this bid_log from being saved:</h2>

      <ul>
      <% @bid_log.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :amount %><br>
    <%= f.number_field :amount %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
