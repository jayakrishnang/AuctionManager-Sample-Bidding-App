<%= render :partial =>'common/details'%>
<hr>
<%= form_for BidLog.new, :controller => "team_owner/bid_logs", :action => "create", :html => { :method => :post} do |f| %>
    <%= f.text_field :amount %>
    <%= f.hidden_field :player_id, value: @user.id %>
    <%= f.submit 'Submit' %>
<% end %>
<a href="<%= team_owner_users_path %>">Back</a>